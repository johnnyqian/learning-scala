package com.horstmann.impatient {
  object Foo {
    def bar {
      println("Foo is in com.horstmann.impatient");
    }
  }
}
package scala.com.horstmann.impatient {
  object Foo {
    def bar {
      println("Foo is in scala.com.horstmann.impatient");
    }
  }
}
